# =====================
# Global / OS / IDE
# =====================
.DS_Store
Thumbs.db
*.sw?
*.suo
*.ntvs*
*.njsproj
*.sln
/.fleet
/.idea
/.vscode
/.zed

# Logs
logs
**/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local env files
.env
.env.*
*.local
!.env.example

# =====================
# Node (both backend & frontend if ever used)
# =====================
**/node_modules

# =====================
# React Vite build
# =====================
**/dist
**/dist-ssr
**/.vite

# =====================
# Laravel
# =====================
.phpactor.json
.phpunit.result.cache
/.phpunit.cache
/auth.json

**/vendor

# Laravel public/build output + dev hot files (only if present)
**/public/build
**/public/hot
**/public/storage

# Laravel storage sensitive/runtime
**/storage/*.key
**/storage/pail

# Laravel runtime cache (optional but safe)
**/bootstrap/cache

# Docker MySQL data
/docker/mysql/data

# Laravel storage (runtime)
**/storage

# Homestead (if used)
Homestead.json
Homestead.yaml
